@model Login;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isTrue = Convert.ToBoolean(ViewData["isTrueCredentials"]);
}

<head>
    <link rel="stylesheet" href="~/css/temp2.css" />
    <link rel="stylesheet" href="~/css/Login.css" />
    <style>
        label span {
            font-size: 12px;
        }
        .help-block ul {
            list-style-type: none;
            font-size: 12px;
        }
    </style>
</head>

<div class="wrapper fadeInDown">
    <div id="formContent">
        <!-- Tabs Titles -->
        <h2 class="active"> Log In </h2>


        <!-- Login Form -->
        @using (Html.BeginForm("Index", "Login", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            if (isTrue)
            {
                <div class="alert alert-danger alert-dismissible fade show hideAfter3Sec" role="alert">
                    <strong>Username and/or Password is Invalid</strong> 
                    <button style="margin-top: 5px; background-color: transparent !important;" type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }
            <label id="lbl" class="form-group required">
                Username<sup style="color:red">*</sup>
                @Html.ValidationMessage("Username", "", new { @class = "text-danger" })
            </label>
            <input asp-for="Username" type="text" class="fadeIn second" name="Username" placeholder="Username">

            <label id="lbl" class="form-group required">
                Password<sup style="color:red">*</sup>
                @Html.ValidationMessage("Password", "", new { @class = "text-danger" })
            </label>
            <input asp-for="Password" type="password" class="fadeIn third" name="Password" placeholder="Password">

            <input style="margin-top: 30px;" type="submit" class="btn btn-primary" id="btnProceed" value="Log In">
        }

        <!-- Remind Passowrd -->
        <div id="formFooter">
            <a class="underlineHover" style="color:black" href="~/Login/SendOtp">Forgot Password?</a>
        </div>

    </div>
</div>